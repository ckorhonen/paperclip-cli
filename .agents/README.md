# Repo-Local Agent Infrastructure

`AGENTS.md` is the canonical root guidance for this repo. Keep durable repo-specific agent rules, commands, and skills under `.agents/`.

- `rules/repo.md` points back to the canonical repo rules.
- `commands/` is reserved for reusable repo-local commands.
- `skills/` is reserved for repeated, fragile workflows that justify a real skill.
